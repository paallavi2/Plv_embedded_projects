# Contributing

- Branch from `main`: `feature/<platform>-<short-name>`
- Commit format: `scope: what changed` (e.g. `stm32: fix I2C timeout`)
- Every project needs a README with hardware, wiring and build steps
- No build artefacts in commits
